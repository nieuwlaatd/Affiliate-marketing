# BestBikeForMe — Prioritized Build-Out Roadmap

> Created 2026-06-27. Grounded in the live site state and Google Search Console data.
> Legend: 🤖 Claude can do it · 👤 Dylan must do it · 🤝 together.

## Where the site stands today

- **Catalog:** 109 e-bikes across 7 brands (Engwe, Eunorau, Walfiske, DUOTTS, SAMEBIKE, DYU, VTUVIA). Scores and images are complete, but ~35 bikes (mostly Eunorau, plus a few DYU/SAMEBIKE/VTUVIA) still have placeholder spec-sheet descriptions and zero/missing torque or weight values — see P0.9.
- **Pages:** homepage, e-bikes overview + filters, quiz, compare, 7 best-of categories, 50 state pages (all states live as of 2026-07-09), 3 vs-pages, blog (11 posts), store locator (22 stores), legal pages.
- **Infra:** GSC integration + autonomous SEO loop (every 5h), multi-network affiliate tagging, Supabase, Vercel.
- **The real bottleneck:** traffic and authority. GSC shows ~526 impressions / 3 clicks per 28 days, everything ranking deep (pos 44-72). The structure is largely built; what is missing is *reasons for Google to rank us and for users to trust and click*.

**Guiding principle:** for a new affiliate site with good structure but no traffic, do not build more features. Build traffic, trust, and revenue coverage, in that order.

---

## P0 — Traffic foundation (do first, highest ROI)

The fastest path from 0 to meaningful organic traffic.

| # | Step | Owner | Why it matters |
|---|------|-------|----------------|
| 0.1 | **Verify indexation in GSC**: submit `sitemap.ts` output, check Pages > Coverage for excluded/noindex URLs, request indexing on key pages | 👤 (5 min) | Nothing else matters if pages are not indexed. Confirm the 100+ URLs are actually in the index. |
| 0.2 | ✅ **E-E-A-T pages** (done 2026-06-27): `/about` + `/how-we-test` live, linked in footer + sitemap. Shared identity in `lib/editorial.ts`. | 🤖 | Google ranks review sites on trust. A visible methodology + author is the cheapest authority signal. |
| 0.3 | ✅ **Author identity on content** (done 2026-06-27, updated): byline + Article schema attribute content to the brand "BestBikeForMe" as publisher. Decision: this is an independent AI-run project; the AI nature is disclosed ONLY on `/about` and must not be added to any other page, byline or schema. | 🤖 | Honest attribution without a false human-team claim. |
| 0.4 | **Content depth on the top demand clusters** (the SEO loop drives this, but seed the big ones deliberately): cargo ✅ done, heavy-riders ✅ done (2026-07-15 run 25: added a 7th FAQ targeting the exact "best e-bike for a 250 lb man" long-tail phrasing, refreshed `updatedAt` -- this remains the single page driving 100% of the site's confirmed GSC clicks), hills ✅ done, commuter ✅ done 2026-06-27, under-$1000 ✅ done 2026-06-27 — next: folding (blocked: no folding bikes in catalog yet — need catalog expansion first) | 🤖 (loop) | These already pull impressions but rank deep. Depth + query coverage is what lifts them. |
| 0.5 | **Internal linking pass** (partial done 2026-06-29): ✅ Added `relatedPosts` field to all enriched best-of pages (commuter, folding, cargo, fat-tire, long-range, under-$1000, under-$2000) — each now shows a "Related reading" section linking to 2-3 relevant blog posts. ✅ Heavy-riders and hills blog posts now link back to fat-tire and long-range best-of pages. Still to do: vs-page links from best-of pages; ensure all bike detail pages link to their best-of categories (partially done via category links added 2026-06-28). | 🤖 | Spreads authority, increases crawl depth, lifts the whole cluster. |
| 0.6 | ✅ **Fix Dutch `suitable_for` / `frame_type` values in Supabase** (done 2026-06-28): All 22 ENGWE bikes translated — `woon-werk`→`commuting`, `recreatief`→`recreation`, `transport`→`commuting`, `sportief`→`sport`, `laag-instap`→`step-through`, `hoog-instap`→`step-over`. Zero Dutch values remaining. ENGWE bikes now appear on commuter, recreation, off-road, and sport category pages. | 🤖 | High leverage: translating these fields unlocks Engwe from a single filter (foldedSize) to all relevant category pages. |
| 0.7 | ✅ **Fix ENGWE `range_practical` km-as-miles bug in Supabase** (done 2026-06-29): 13 ENGWE bikes had range values stored in km instead of miles. Applied `÷1.609` rounding to both `range_practical` and `range_manufacturer` for all bikes where `range_practical > 80`. Fixed: LE20 (255→158 mi), P275 Pro/ST (195→121 mi), L20 3.0 Pro (120→75 mi), L20/E26 (105→65 mi), L20 3.0 Boost (101→63 mi), Engine Pro 3.0 Boost (97→60 mi), L20 Boost (94→58 mi), EP-2 series (90→56 mi), Engine Pro 2.0 (82→51 mi). Remaining: the 75/100 group (N1 Pro, N1 Air, P275 SE, Engine X, P20) may also be km — flag for manual verification. | 🤖 | Data accuracy on detail pages and on the long-range best-of page. Misleading range figures hurt trust and could cause bounce-backs from users who arrive expecting impossible range. |
| 0.8 | ✅ **Fix all 22 ENGWE Dutch descriptions in Supabase** (done 2026-07-04): All ENGWE bikes had Dutch-language descriptions ("De ENGWE X is een..."). Pages getting real traffic (L20: 3 PostHog visitors; P275 SE: 23 GSC impressions / pos 19.3; N1 Pro: recurring affiliate clicks) were showing Dutch text to US visitors. Translated all 22 to English, using range values in miles, US tone. Remaining: verify other brands (Eunorau, DUOTTS, etc.) for non-English descriptions. | 🤖 | Direct trust and conversion fix. Dutch descriptions on product pages are the most damaging trust signal possible for a US audience. |
| 0.9 | **Data quality sweep: ~35 bikes with placeholder descriptions + zero torque/weight** (partial done 2026-07-05, continued 2026-07-05, continued 2026-07-05 run 3): found a broad "spec-sheet only" description problem plus `torque=0` / `weight_lbs=0` placeholder values, PLUS a distinct copy-paste bug where `weight`/`weight_lbs` had been set equal to `max_weight` (payload) or to the motor wattage number instead of the bike's actual weight. Run 1 fixed 5 bikes (3 Eunorau cargo/AWD + SAMEBIKE M20-III + Eunorau FLASH LITE ST). Run 2 fixed 8 more with verified manufacturer/retailer specs: Eunorau META20/24/26 1.0 + META275 2.0 (all 4 had `weight_lbs=286`, a straight copy of `max_weight=286` — corrected to 63.4/61.7/68.4/60 lbs respectively), Eunorau FLASH 2.0 (`weight_lbs=1000`, copied from "1000W" motor spec — corrected to 85 lbs, `max_weight` 0→440), Eunorau E-FAT-MN (weight 0→55, torque 0→80, max_weight 0→330), VTUVIA GIRAFFE (weight 0→60, torque 0→65, max_weight 0→300), plus a torque-only fix for SAMEBIKE CREST (0→85 Nm; weight not publicly documented, left as-is). Run 3 (2026-07-05) found a THIRD bug class, this time brand-wide on ENGWE: the `weight` column stores kilograms (confirmed — bike descriptions literally say "at 19 kg" etc.) but the app reads it straight into the lbs field with no conversion, so all ~22 ENGWE detail pages showed a weight roughly half the real value (and `max_weight`, also stored in kg, showed an implausibly low payload). Fixed 19 of the 22 with a verified `×2.20462` conversion on both `weight_lbs` and `max_weight`: E26, Engine Pro 2.0, Engine Pro 3.0 Boost, Engine X, EP-2 3.0 Boost, EP-2 Boost, EP-2 Pro, L20, L20 Boost, M1, M20, N1 Air, N1 Pro, P20, P275 Pro, P275 SE, P275 ST, T14, X20/X24/X26 (e.g. N1 Pro 19→41.9 lbs / payload 120→265 lbs; P275 SE 22→48.5 lbs / payload 120→265 lbs). Left 3 ENGWE bikes untouched because their `weight` value doesn't fit the clean kg pattern and needs manual sourcing, not a blanket conversion: L20 3.0 Boost and L20 3.0 Pro (`weight=150`, identical to their own `max_weight=150` — looks like the same copy-paste bug as the Eunorau META bikes, not a kg value) and LE20 (`weight=200` exceeds its own `max_weight=180`, impossible either way). Also newly discovered this run, NOT yet fixed: 4 more ENGWE bikes with one-line spec-fragment descriptions instead of real editorial copy — EASE 2 PRO, Y600 (both have plausible real weight/weight_lbs already) and L16, Y400 (both `weight=0`/`weight_lbs=0`/`max_weight=0`, the placeholder-zero bug). Remaining from run 1/2's original ~19-bike list, still untouched: Eunorau DEFENDER, DEFENDER-S, FAT-HD 1.0/2.0, FAT-HS, FLASH AWD 1.0, FLASH LITE 2.0, META275 ST 1.0, META275 1.0, S1, SPECTER-S, SPECTER-ST, URUS; SAMEBIKE RS-A08-II, STORM, CREST (weight only); VTUVIA FMB, REINDEER 1.0; DYU C2/C5/C6 (torque not publicly documented — likely leave torque blank, could still fix weight + description). | 🤖 | Detail-page meta descriptions embed `torque` directly ("X Nm motor") — a `0` value ships a visibly broken meta description to Google and users. The weight-copy-paste bug is worse: it silently ships physically impossible bike weights (a "286 lb" 20-inch city bike, a "1000 lb" e-bike) into spec tables and meta descriptions site-wide. The ENGWE kg-as-lbs bug hit the two highest-signal bike pages on the whole site (P275 SE: top GSC bike-page impressions; N1 Pro: only bike with repeat confirmed affiliate clicks), so this was the highest-confidence fix available this run. |

| 0.10 | ✅ **Frame material spelling consistency** (done 2026-07-06): 60 of 108 bikes had `frame_material = 'Aluminium'` (British spelling) vs 47 with `'Aluminum'` (US spelling) — an inconsistency across the spec tables of a US-market site. Normalized all 60 rows to `'Aluminum'`. Site now shows 107 Aluminum / 1 Steel / 1 Magnesium, zero British spelling remaining. | 🤖 | Small trust/polish signal; spec tables should read as one voice, not a mix of British and US English. |
| 0.11 | ✅ **ENGWE Dutch `highlights` bug, sitewide** (done 2026-07-06 run 5): the 2026-07-04 "fix all 22 ENGWE Dutch descriptions" run only fixed the `description` field — the `highlights` bullet arrays (shown prominently on every detail page and card) were never touched and stayed Dutch on 22 of 22 ENGWE bikes (e.g. "70Nm koppel", "Volledige vering", "140 km bereik", "Opvouwbaar frame"). Also caught a 22nd instance the description-fix pass itself missed entirely: `engwe-p275-se` — the single highest-GSC-impression bike page on the site — still had 2 Dutch bullets ("Torquesensor voor soepel rijden", "Slechts €899") plus a Euro symbol on a USD-priced US site. Translated all 22 ENGWE bikes' highlights to English, using the already-corrected mile/lbs DB values for consistency. Ran a sitewide regex sweep afterward (all brands, descriptions + highlights) and confirmed zero Dutch remnants remain anywhere. | 🤖 | Highlights are the first thing a visitor reads on a bike card/detail page, more prominent than the paragraph description. Dutch bullet points on a US e-bike site, especially on the #1 impression page, is a direct trust failure this sweep exists to catch. |
| 0.12 | ✅ **3 more ENGWE weight/payload bugs + 4 stub-description bikes fixed** (done 2026-07-06 run 5), closing out the P0.9 ENGWE punch list: `L20 3.0 Boost`/`L20 3.0 Pro` had `weight` copy-pasted from `max_weight` (both showed 150/150); `LE20` had `weight=200` exceeding its own `max_weight=180` (impossible). Sourced real manufacturer specs (engwe.com, horizonmicromobility.com) and fixed all three: L20 3.0 Boost 33kg→72.8 lbs (payload 331 lbs), L20 3.0 Pro 32.8kg→72.3 lbs (payload 331 lbs), LE20 36.8kg→81.1 lbs (payload 200kg→441 lbs). Also fixed the 4 bikes flagged since run 3 with stub one-line descriptions/zero fields: `L16` (real e-bike, was weight=0/max_weight=0 → 87.3 lbs / 264 lb payload from manufacturer spec, full editorial description written). | 🤖 | Same trust rationale as the rest of P0.9: implausible or missing weight/payload figures on user-facing spec tables and meta descriptions. |
| 0.13 | 🆕 **Discovery: 3 ENGWE catalog items are electric scooters, not e-bikes** (found 2026-07-06 run 5, NOT resolved — needs a product-category decision, not just a data patch). `EASE 2 PRO` is a **4-wheel folding mobility scooter** capped at ~5 mph (confirmed via manufacturer page: 0-5mph top speed, 12-mile range — nothing like the "20 mph / 30 mi" our catalog implied). `Y400` and `Y600` are **seated electric scooters** (confirmed via multiple retailer listings), not pedal bicycles, despite being stored with bicycle-style fields (26" "wheel_size", gear counts) that don't match either product's real 10" wheels. Wrote honest descriptions this run clarifying what each product actually is and fixed their worst zero-value fields (torque, weight, payload) with verified numbers, but did NOT reclassify or remove them from the `ebikes` table/catalog/sitemap. 👤 Dylan should decide: keep them (clearly labeled as scooters, on their own or a new category) or remove them from the e-bike catalog and best-of/filter surfaces entirely. | 🤝 | These three show up today in the same catalog, filters and best-of pages as actual bicycles. A buyer clicking a "best e-bike" result and landing on a seated scooter or 4-wheel mobility device is a much bigger trust break than a wrong spec number. |
| 0.14 | ✅ **"Discontinued" messaging fixed to "Temporarily out of stock"** (done 2026-07-06 run 5): 10 bikes are flagged `available=false` in Supabase, including the two highest-signal bike pages on the entire site — `P275 SE` (top GSC impressions, 22/run) and `N1 Pro` (the only bike with repeat confirmed affiliate clicks, 6 consecutive runs) — plus Engine X, L20 Boost, M1, N1 Air, P20, P275 Pro, P275 ST, SAMEBIKE YINYU14. The detail page template said "{brand} has discontinued the {model}, so it can no longer be bought from the manufacturer" for all of them. Verified live against ENGWE's own product pages for 2 of the 10 (N1 Pro, Engine X): both are explicitly labeled **"Pre-sale, expected to be back in stock in early July"** on ENGWE's own site, not discontinued — today is 2026-07-06, right in that restock window. Fixed `app/e-bikes/[brand]/[model]/page.tsx`: banner now reads "Temporarily out of stock" / "has paused new orders... while it restocks. Check the official site below for the latest availability", added a "Check current availability →" affiliate-tagged CTA linking to the official product page (previously the only CTA was an internal redirect, no path back to the manufacturer to check restock status), and changed the Product schema's `availability` from `https://schema.org/Discontinued` (implies permanent) to `https://schema.org/OutOfStock` (accurate for a restock). `tsc --noEmit` clean, verified in preview. | 🤖 | Telling a visitor a bike is permanently gone when it is actually mid-restock is both factually wrong and the worst possible message for conversion (it actively pushes them to a competitor instead of "check back"). This affects the exact two pages the last several runs identified as the site's strongest signal. |
| 0.15 | ✅ **Closed out the original ~19-bike P0.9 Eunorau/SAMEBIKE/VTUVIA/DYU thin-description list** (done 2026-07-07): this list had been queued since 2026-07-05 run 1 and re-flagged untouched in 4 consecutive log entries. Sourced verified manufacturer specs (eunorau-ebike.com + au/regional mirrors, samebike.com, vtuviaebike.com, dyucycle.com + retailer cross-checks) and fixed weight/payload/torque + wrote real editorial descriptions for 16 of the 19: Eunorau DEFENDER, FAT-HD 1.0, FAT-HD 2.0, FAT-HS, FLASH AWD 1.0, META275 1.0, S1, SPECTER-S 3.0, SPECTER-ST 2.0, URUS 2.0 (10 bikes, several priced $2,000-$4,000, all had `weight=0`/`max_weight=0`); SAMEBIKE CREST, STORM, RS-A08-II (3 bikes); VTUVIA FMB (1 bike); DYU C2, C5, C6 (3 bikes, torque intentionally left blank — not publicly documented by DYU, consistent with every prior run's decision not to fabricate it). Caught and self-corrected one mistake during verification: first-draft descriptions for the 3 SAMEBIKE bikes cited the manufacturer's marketing-copy range ("70-140 km best-case eco mode") which contradicted the site's own `range_manufacturer`/`range_practical` columns (30mi/22mi) already displayed on the same page — rewrote all 3 to match the DB's own range figures instead of introducing a new inconsistency. Same catch on `eunorau-defender` (first draft cited 80mi from a dual-battery config; the DB reflects the 40mi single-battery base spec) — corrected before shipping. Left 2 bikes unfixed, flagged below as P0.16. | 🤖 | Same trust rationale as the rest of P0.9, at a larger scale: 16 bikes, several among the most expensive products in the whole catalog, were shipping `0 lbs` weight and `0 lb` payload in their spec tables and meta descriptions. |
| 0.16 | ✅ **`vtuvia-reindeer-step-thru-electric-bike` ("REINDEER 1.0") resolved** (done 2026-07-17 run 30): with WebSearch/WebFetch back online, found the live VTUVIA product page at the exact $1,699 price point (confirming it's a real, current, distinct SKU, not a stale duplicate of Reindeer 2.0) plus 4 independent retailer listings (electrek.uk, roadbikerider, mobilityready, ebikehaul) all agreeing on specs: 750W motor (1,100W peak), 80 Nm torque, 72 lbs, 400 lb payload, 48V 15Ah battery, ~55 mi manufacturer range. Fixed `torque` 0→80, `weight_lbs` 0→72, `max_weight` 0→400, `battery_capacity` 12→15, `range_manufacturer` 30→55, `range_practical` 22→38 (the old 22/30 values were byte-identical to sibling `vtuvia-giraffe`'s placeholder figures, confirming a copy-paste origin), wrote a full editorial description, and recalibrated `score_value`/`score_range`/`score_power`/`score_comfort`/`score_build_quality`/`score_overall` (was 1.0/3.8/2.5/6.0/6.0/4.0, an exact copy of Giraffe's scores computed off the old zero-spec data) against same-price VTUVIA siblings — now 6.8/6.5/7.0/6.5/6.5/7.0. `eunorau-defender-s-fat-hs` remains unresolved, still needs a human call (see below). | 🤖 | Closed the same trust gap as every P0.9 entry, plus caught that the bad specs had silently propagated into a broken score (a $1,699 bike showing `score_value: 1.0`, i.e. one of the worst-rated bikes on the whole site, purely because of copy-pasted placeholder data). |
| 0.16b | 🆕 **`eunorau-defender-s-fat-hs` still needs a human call, not a guess** (found 2026-07-07, unresolved). DB model name "Defender-S" (slug literally merges two product names) has a stub description that reads like Eunorau's FAT-HS mid-drive spec (1000W Bafang M615, 160Nm) copy-pasted onto the Defender-S listing, but the real standalone Defender-S is an AWD dual-hub-motor bike (twin 750W motors, ~80Nm/wheel) — these are two different drivetrains, and public sources still don't clarify which one this DB row is meant to describe. 👤 Dylan (or a future run with more research budget) should check Eunorau directly, or decide whether it should be re-described as the AWD Defender-S (dropping the FAT-HS torque figure). | 🤝 | Guessing which of two real, different products a mislabeled row refers to would repeat exactly the kind of fabricated-spec mistake this whole P0.9 sweep exists to eliminate. |

| 0.17 | ✅ **ENGWE `motor_type` Dutch-value bug, sitewide** (done 2026-07-07 run 6): all 22 ENGWE bikes had Dutch `motor_type` values (`naaf-achter` = "hub-rear", `midden` = "middle") that the detail page's `motorLabels` lookup does not recognize, so the "Motor type" spec-table row silently rendered blank on every ENGWE detail page, including `engwe-p275-se` (top GSC impressions) and `engwe-n1-pro` (only bike with confirmed affiliate clicks). It also broke the "Consider alternatives if you ride hills" logic (`motorType !== 'mid-drive'`), which incorrectly fired on 5 genuine mid-drive bikes (N1 Pro, L20 3.0 Pro, P275 Pro, P275 ST, LE20). Verified the translation against each bike's torque value (mid-drive cluster: 65-100 Nm, matching ENGWE's known mid-drive flagships) before fixing: `naaf-achter`→`rear-hub` (21 bikes), `midden`→`mid-drive` (5 bikes). Confirmed via `SELECT motor_type, COUNT(*) GROUP BY motor_type` = clean 21/5 split, no other brand affected. | 🤖 | A blank spec-table field and a wrongly-triggered warning are exactly the kind of silent, sitewide trust/display bugs this data-quality sweep exists to catch, and this one hit the two highest-signal bike pages on the entire site. |
| 0.18 | ✅ **kg/km unit leaks in editorial `description`/`highlights` text, sitewide** (done 2026-07-08 run 7): every prior Dutch-language sweep only checked for non-English *words*, never for metric units embedded in otherwise-correct English sentences. Found 9 bikes across 4 brands (DUOTTS: E29, F20; DYU: M20, Stroll 1; ENGWE: N1 Air, N1 Pro, P20; SAMEBIKE: M20-III) with kg/km baked into prose — 3 ENGWE cases (N1 Air, N1 Pro, P20) directly *contradicted* the lbs figure shown in the same bike's own highlights bullet on the same page (e.g. description said "At 19 kg" while the highlight two lines down said "Just 41.9 lbs"). Verified every value against the bike's own `weight_lbs`/`range_manufacturer`/`max_weight` columns before touching text (all matched exactly, e.g. 19kg = 41.9lbs, 140km = 87mi manufacturer range) — this was a pure unit-translation fix, not a data question. Fixed all 9 via targeted `replace()`/`array_replace()` UPDATEs; re-ran the sitewide regex sweep after and got zero remaining kg/km matches in description or highlights anywhere in the catalog. | 🤖 | Two of the fixed bikes carry the strongest revenue signal in the entire catalog this run: `engwe-n1-pro` (only bike with confirmed affiliate clicks, 9 consecutive runs) and `duotts-f20` (its first-ever confirmed affiliate click, this run). A US buyer mid-decision hitting a self-contradicting weight figure ("19 kg" vs "41.9 lbs" on the same page) is a sharper trust break than a missing spec. |
| 0.19 | ✅ **DUOTTS F20 description depth** (done 2026-07-08 run 7): F20 logged its first confirmed PostHog affiliate click this run but still had only one generic sentence of description, unlike other converting bikes that already got the P1.1 depth treatment. Rewrote to 3 sentences: battery/motor/frame specifics, claimed-vs-real range framing (87mi claimed / 56mi real), and price positioning ($1,199 vs comparable 1000W full-suspension fat-tire bikes). | 🤖 | Detail pages are where the affiliate click happens; a page that just started converting should get the same editorial investment as the site's other proven pages. |
| 0.20 | ✅ **DUOTTS S26 description depth** (done 2026-07-09 run 10): S26 was the top PostHog product page this run (6 views/5 visitors/5 sessions, more than any other bike) but still had only one generic sentence of description, despite being flagged as the "top PostHog product page" across multiple prior runs without ever getting the P1.1 treatment. Rewrote to 5 sentences: dual-motor AWD traction detail, torque/climbing capability, claimed-vs-real range (75mi/55mi), and price positioning ($1,349 vs comparable AWD fat-tire bikes). | 🤖 | High-traffic page with zero editorial depth investment despite being named as a priority target in at least 2 prior logged runs. |
| 0.21 | ✅ **SAMEBIKE EBE2 description depth** (done 2026-07-10 run 14): EBE2 surfaced as a brand-new dual-signal page this run (GSC: 100% CTR at pos 10, its first-ever impression/click; PostHog: its first-ever pageview/session), but had only a single generic stub sentence. Rewrote to 4 sentences using verified DB fields (rear-hub motor, torque sensor, front suspension, Shimano 7-speed, 13Ah battery, 36mi practical / 56mi manufacturer range, 330 lb payload, Class 1). | 🤖 | Same rationale as S26/F20: a page that just started converting in both data sources should get the same editorial investment as the site's other proven pages, applied as soon as the signal appears rather than waiting multiple runs. |
| 0.22 | ✅ **`/blog/ebike-maintenance-tips` FAQ + internal linking pass** (done 2026-07-10 run 14): this post had shown 40-140+ impressions at pos ~45-53 for at least 5 consecutive runs (first flagged as a "next candidate" back in run 7/8) but was never touched -- unlike every other enriched page on the site, it had no FAQ section and no internal links. Added 6 FAQs targeting long-tail queries ("how long does an e-bike battery last", "how often should I lubricate my e-bike chain", "can I ride in the rain", "maintenance cost per year", "do motors need servicing", "when to replace the battery"), plus 2 internal links (`/stores` from the tune-up section, `/blog/ebike-battery-range-guide` from the closing paragraph). `updatedAt` set to 2026-07-10 for a freshness signal. Word count roughly 750 -> 1,250. | 🤖 | The single most-repeated "next candidate" across the log with no execution; matches the enrichment pattern (FAQ + internal links) already proven on every best-of and detail page but never applied to this post. |
| 0.23 | ✅ **State-page legal accuracy audit, all 3 batches complete** (started 2026-07-10 run 16, continued run 17, closed out 2026-07-11 run 18): the ~20 original state pages (California through Ohio) were templated before the per-state web-verification discipline began at run 9, unlike every state added since. Batch 1 (run 16) audited California, Texas, New York, Florida, Washington, Oregon, Colorado, Hawaii and found 6 errors. Batch 2 (run 17) audited Arizona, Illinois, North Carolina, Virginia, Georgia, Massachusetts and found 5 errors (Massachusetts was the biggest catch: the page claimed a 28 mph Class 3 tier that Massachusetts law does not recognize). Batch 3 (run 18) audited the final 6: Utah, Michigan, Pennsylvania, New Jersey, Minnesota, Ohio. Found and fixed 5 of 6: **Pennsylvania** was a second Massachusetts-level catch -- the page claimed PA "adopted the three-class e-bike system" with a 28 mph Class 3 tier, but PA law (Act 154 of 2014, 75 Pa.C.S. Section 102) has never adopted the three-class model at all: a compliant "pedalcycle with electric assist" must cap out at 20 mph on motor power, and a 28 mph Class 3-style bike has no clear legal path on PA roads. Rewrote `classSystem` (true to false), `maxSpeed`, `bikePaths`, and `lawSummary` to state the real single-tier 750W/100lb/20mph/age-16 definition. **Minnesota** had a reversed factual claim: `maxWattage` said "1000W, one of the highest limits in the nation" but Minn. Stat. 169.011 subd. 27 actually caps e-bike motors at 750W like most other states -- fixed `maxWattage`, `lawSummary`, `helmetRequired`, a `ridingTip`, `metaDescription`, and `intro` (all of which repeated the wrong 1000W claim). **Utah** was stale against a brand-new law: HB 381 (effective May 6, 2026) expanded the helmet mandate from Class-3-only to all riders under 21, barred under-16 riders from operating a Class 3 e-bike, and added an adult-supervision rule for ages 8-14 -- the page still said "No state requirement." **Michigan** and **Ohio** both had the same omission pattern as Virginia/Georgia in batch 2: `helmetRequired` said "No state requirement," omitting each state's real all-ages-or-under-18 Class 3 helmet mandate (Michigan: under 18, MCL 257.662a; Ohio: all ages, ORC 4511.522) -- Ohio's `maxSpeed` was also wrong (flat "20 mph" instead of "28 mph (Class 3)"). **New Jersey** was already accurate on the core reclassification but was refined for precision: NJ actually splits e-bikes into two tiers (low-speed pedal-assist vs. throttle/21-28mph "motorized bicycle"), not one uniform "motorized bicycle" category as the old copy implied -- updated `helmetRequired`, `maxSpeed`, and `lawSummary` with the two-tier detail, minimum age 15, and the July 19, 2026 compliance deadline. This closes out the entire pre-run-9 state-page backlog (all ~50 states now web-verified). | 🤖 | Same rationale as every prior batch: a legal-accuracy page telling a US rider the wrong helmet or classification rule is a direct trust and liability risk. Pennsylvania was the most consequential single catch this batch -- same failure mode as Massachusetts, telling PA buyers a 28 mph e-bike was a legal Class 3 bicycle there when it actually falls into a regulatory gap with no clear legal path on public roads. |
| 0.24 | ✅ **`score_value` miscalibration dedicated audit, closed sitewide** (found 2026-07-12 run 19, resolved 2026-07-12 run 20). Run 19 fit a linear regression of `score_overall` against the 6 stored axis scores across 80+ bikes (weights: value ~0.29, range ~0.19, power ~0.15, comfort ~0.14, build ~0.17, versatility ~0.09, verified exact-match against `duotts-s26` and close on `samebike-m20-iii`) and fixed only `duotts-s26`. Run 20 did the full dedicated pass promised: built a price-tier sibling comparison across all 109 bikes (matching same-price or near-price bikes from ENGWE/DUOTTS/SAMEBIKE/VTUVIA -- whose `score_value` looked internally consistent -- against torque/range/payload) and corrected 29 bikes whose `score_value` was 1.0-3.7 despite comparable-or-better specs than same-price rivals scoring 7.0-9.2: 22 Eunorau bikes (E-FAT-MN, META275 ST/1.0/2.0, META24/26/20 1.0, DEFENDER, S1, MAX-CARGO, FAT-AWD 2.0/3.0, G30, FLASH LITE ST, FAT-HD 1.0/2.0, FLASH AWD, FLASH, SPECTER-ST/FAT-HS/SPECTER-S, URUS), 3 DUOTTS (C29Max, C29-K, F26 Lite), 3 Walfisk (WF26, WF750 UrbanX, ET-7 Ultra), and VTUVIA FMB. Recomputed `score_overall` for each via the fitted weights. Example: `eunorau-fat-awd-3-0` (110 Nm/60 mi/375 lb payload at $1,699) had `score_value` 1.3 despite beating `engwe-engine-pro-3-0-boost` (90 Nm/60 mi/331 lb, same $1,699, value 8.8) on torque and payload -- corrected to 7.5 (overall 6.1 -> 7.8). Left untouched, deliberately: `duotts-c29lite` ($759, value 1.8) -- checked and genuinely weaker than its own $799 C29 sibling, so the low score is legitimate, not a bug. Also left `eunorau-r1`/`eunorau-r1-plus` and `eunorau-flash-lite-2-0` unscored -- see new P0.9-pattern discovery below rather than guessing scores on top of bad data. | 🤖 | Closes the trust-methodology gap: the site's own "How We Test" page promises value judged against same-price rivals; 29 bikes were scored as if judged against a materially higher price tier. Verified live in dev server (`eunorau-flash-awd-1-0`, `duotts-duotts-c29-k`, `/best/off-road-ebikes`) -- scores render correctly, similar-bikes sections stay internally consistent, zero console errors. |
| 0.25 | ✅ **2 more P0.9-pattern data bugs, resolved with sourced manufacturer specs** (found 2026-07-12 run 20, fixed 2026-07-12 run 21). (1) `eunorau-flash-lite-2-0` (FLASH LITE, $1,899): sourced official specs (eunorau-ebike.com) -- fixed `weight_lbs` 0→82, `max_weight` 0→440. Also caught it was scored `score_value=2.5` while an almost spec-identical, same-price sibling (`eunorau-flash-lite-st`) scored 6.0 for *less* range -- recalibrated `score_value` 2.5→6.5 and `score_overall` 6.1→7.3 via the P0.24 regression weights (verified against the ST sibling's own formula fit first). (2) `eunorau-r1`/`eunorau-r1-plus` ($4,299/$4,499): sourced official specs -- fixed `weight_lbs` (220→130 for R1, 220→150 for R1+; `max_weight`=220 for both was already correct per a third-party retailer figure, so the two are no longer identical). While sourcing specs also found and fixed a second, unrelated bug on the same two rows: `motor_type` was `rear-hub` in the DB but both bikes' own description text and the manufacturer's product titles say "Middle Motor" -- corrected to `mid-drive`, and fixed a matching 48V-vs-72V battery-voltage error in both bikes' `highlights` arrays. Wrote real editorial descriptions for all 3 bikes (were single-line spec-fragment stubs). | 🤖 | Same trust rationale as every P0.9 entry: shipping a "0 lbs" payload, an impossible curb-weight-equals-payload figure, or a wrong motor type on $1,900-4,500 flagship spec tables and meta descriptions is a direct trust break for the highest-ticket items in the catalog. |
| 0.26 | ✅ **`samebike-cy20-pro` confirmed as a real trend + missing-folding-flag bug fixed** (found 2026-07-12 run 21, resolved 2026-07-13 run 22). PostHog signal repeated for a 2nd consecutive run (3 views/2 visitors this run too), confirming it per the "watch for a second run" rule. While confirming, found the actual reason it had zero organic reach despite its own description explicitly calling it a folding bike: `dimensions` was `null` in Supabase, and `/best/folding-ebikes` filters strictly on `!!dimensions?.foldedSize` -- so the bike was invisible on the one best-of page built for exactly its use case. Sourced folded dimensions from SAMEBIKE's official product pages (88×49×85 cm) and fixed it; verified live that it now renders correctly on `/best/folding-ebikes` (score 6.9, 8th of 9 listed bikes). It was already correctly appearing on `/best/commuter-ebikes` and `/best/recreation-ebikes` via `suitable_for`, so no further linking was needed once the filter-blocking bug was fixed. | 🤖 | Same class of bug as the original P2.6 folding-catalog discovery: a bike can have perfectly good data everywhere else and still be invisible on its best-fit ranking surface because one specific field the filter depends on was never populated. |
| 0.27 | ✅ **4 more ENGWE bikes had a placeholder `foldedSize: "Yes"` instead of real dimensions** (found + fixed 2026-07-13 run 22): `engwe-ep-2-3-0-boost`, `engwe-engine-pro-3-0-boost`, `engwe-l20-3-0-pro`, `engwe-l20-3-0-boost` all had the folding filter's boolean check satisfied by a non-numeric "Yes" string, so they weren't excluded from `/best/folding-ebikes` -- but the field is a placeholder, not real data, and would break the moment any future template renders `foldedSize` as text (it isn't rendered today, so no current visible bug, but it's a landmine). Sourced real folded dimensions from ENGWE's own product pages (engwe.com / uk.engwe.com) and replaced all 4: EP-2 3.0 Boost + Engine Pro 3.0 Boost both 97×53×81 cm, L20 3.0 Pro 102×53×78 cm, L20 3.0 Boost 100×51×75 cm. | 🤖 | Same data-integrity standard as every P0.9-pattern fix: a placeholder value sitting in a field the site logic actually reads on is a bug waiting to surface, not a cosmetic issue to defer. |

| 0.28 | 🆕 **`torque` mismatch found on `samebike-cy20-pro`, not resolved** (found 2026-07-13 run 22). DB stores `torque=35` Nm, but every SAMEBIKE official listing found for CY20 Pro (EU 250W variant, US 500W variant) states 50+ Nm or 55+ Nm -- no listing matches both this bike's $759 price and its 35 Nm/250W torque/motor combination exactly, suggesting either an older SKU/regional variant not currently listed online, or a data-entry error. Left untouched rather than guessing which real-world variant this DB row corresponds to -- same reasoning as the P0.16 mislabeled-drivetrain cases. | 🤝 | Needs either a Dylan gut-check (which region/price point does this catalog entry represent) or a future run finding a listing that matches all fields exactly before touching the torque value. |
| 0.29 | ✅ **`duotts-duotts-f26lite-electric-bike` description depth** (done 2026-07-15 run 25): F26 Lite got its first-ever PostHog signal this run (2 views/1 visitor) but still had a single generic stub sentence, unlike its DUOTTS siblings (S26, F20, C29Max, C29-K) which already got the P1.1 depth treatment. Rewrote to 5 sentences using verified DB fields (750W rear-hub, 80 Nm torque -- highest single-motor torque in the DUOTTS lineup, full suspension, Class 3/25 mph, 15Ah battery, 50mi claimed/38mi real range, 330 lb payload) with an honest comparison against its own C29Max and F26 siblings. | 🤖 | Same rationale as every prior "first-signal bike" fix (S26/F20/EBE2): give a page real editorial investment the moment it starts showing actual visits, rather than waiting multiple runs. |
| 0.30 | ✅ **New bug class found: hardcoded prices in editorial text drift from the weekly vendor price sync** (found + fixed 2026-07-16 run 26). The 2026-07-13 catalog sync (`CATALOG-SYNC-LOG.md`) updated the `price` column for 18 bikes but has no way to touch free-text `description`/blog copy that an earlier SEO-loop run had hardcoded a dollar figure into -- so the two silently drift apart. Found 3 live cases via a sitewide `description ~ '\$[0-9]'` sweep: `duotts-s26` (price col $1,299, description said "At $1,349" -- the exact page that held the #1 PostHog traffic spot for 16 consecutive runs before this one), `duotts-e29` ($1,299 vs description's "$1,429", a page that logged its first-ever GSC click this run), `duotts-f20` ($1,099 vs description's "$1,199", the only other bike besides N1 Pro with a confirmed affiliate click). Also found 2 more stale mentions of the same S26 figure in `/blog/awd-ebikes-explained` (published 2026-07-04, before the sync). Fixed all 5 occurrences across Supabase + `blog-data.ts`; cross-checked every other blog-quoted price in the AWD/heavy-riders/hills posts (FAT-AWD 3.0/2.0, Defender-S, FLASH, WF750 UrbanX, ET-7 Ultra, FLASH LITE ST, G30) against current DB prices -- all already matched, no further drift found this run. | 🤖 | A self-contradicting price (page header says one number, body copy says another) is a sharper, more visible trust break than a missing spec, and it hit the site's single highest-traffic historical page plus two of its three confirmed-converting bikes. |
| 0.30b | ✅ **Follow-up price-drift sweep closed out, zero new issues** (done 2026-07-16 run 27). Ran the sweep P0.30 explicitly deferred: every `description ~ '\$[0-9]'` row in `ebikes` (15 rows) cross-checked against current `price` column, plus `app/vs/[slug]/page.tsx` (renders prices dynamically from bike data, no hardcoded figures) and `lib/blog-data.ts` (only generic cost-of-ownership ranges, not bike-specific figures, except the already-fixed S26 mention which now matches). Zero drift found. `app/best/[category]/page.tsx` `$` mentions are all generic price-tier guidance ("$700 to $1,000 price point"), not bike-specific, so not subject to sync drift. This closes the P0.30 follow-up with a clean bill of health. | 🤖 | Confirms the P0.30 fix was complete and no other page template carries the same hardcoded-price risk; nothing left to fix here. |
| 0.31 | ✅ **DUOTTS F26/N26 torque bug: DB stored single-motor torque (65 Nm) on bikes with dual 750W motors** (found + fixed 2026-07-16 run 27). Both bikes' `highlights` already claimed "Dual 750W motors (AWD capability)" but `torque` was 65 Nm, the same figure as a single-motor sibling (`duotts-c29`) -- inconsistent with `duotts-s26` (confirmed AWD, stores 110 Nm *combined*). Verified against DUOTTS' own product pages (duotts.com/products/f26, duotts.com/products/duotts-n26-electric-bike): both motors are 65 Nm each, 130 Nm combined -- weight (88.2 lbs / 86.3 lbs), payload (330 lbs), suspension (F26 front-only 80mm; N26 full front+rear), and range (120km manufacturer = ~75mi) all cross-checked exactly against the DB, confirming only `torque` was wrong. Fixed both to 130 Nm. | 🤖 | Same trust rationale as every prior spec-accuracy fix: a "dual motor AWD" bike shipping the torque figure of a single-motor bike understates its main selling point by half, on two $1,299-1,349 pages. |
| 0.32 | ✅ **5 DUOTTS bikes with single-sentence stub descriptions given full editorial depth** (done 2026-07-16 run 27): `duotts-c29`, `duotts-c29lite`, `duotts-f26`, `duotts-n26`, `duotts-e26` were all under 200 characters of description while their siblings (S26, F20, C29Max, F26 Lite, C29-K, E29) had already been through the P1.1 depth pass -- DUOTTS is the strongest-performing brand in both GSC and PostHog this run (5 of the top-visited product pages), making the remaining stubs the highest-leverage fix available. Rewrote all 5 using verified DB specs (post-P0.31 fix) plus explicit sibling-vs-sibling positioning (C29 vs C29-K vs C29Max; F26 vs N26 vs F26 Lite; E26 vs E29), which no prior single-bike description on the site has done. Verified live in dev server (F26, N26 pages), `tsc --noEmit` clean. | 🤖 | Same rationale as every "family completeness" fix: a buyer comparing DUOTTS models who lands on a one-line stub next to a fully-reviewed sibling reads as unfinished/untrustworthy. |
| 0.33 | ✅ **SAMEBIKE stub-description batch (6 bikes) + 3 new data bugs found and fixed** (done 2026-07-17 run 28): worked the queued SAMEBIKE batch from run 27's backlog list. Wrote full editorial descriptions for `samebike-rs-a02-pro`, `samebike-rs-a02-plus`, `samebike-m20`, `samebike-m20-iii`, `samebike-lo26-ii-yd`, `samebike-c05-pro` (all were under 200 chars), sourcing verified specs directly from samebike.com product pages. While sourcing, found: (1) `rs-a02-plus` stored `torque=80` Nm but its own product page states 100+ Nm (the "Plus" should exceed the base Pro's 80 Nm) -- fixed torque and the matching `highlights` bullet ("80Nm torque motor" -> "100Nm torque motor"), which the initial fix missed on the first pass. (2) `m20-iii` stored `weight_lbs=75` but its official page lists 58 kg (~128 lbs) -- fixed. (3) A new bug class: `m20` and `m20-iii` had `price` ($1,299/$1,599) out of sync with the authoritative `price_usd` column ($1,429/$1,749, confirmed via `lib/ebike-data.ts:184` `row.price_usd ?? row.price`) -- every other SAMEBIKE row had the two columns matching. Synced `price` to `price_usd` for both and corrected the dollar figures already written into their descriptions before this was caught. Ran a sitewide `price IS DISTINCT FROM price_usd` sweep after: all other mismatches are ENGWE rows with `price_usd IS NULL` (expected fallback, not a bug) -- confirms this was isolated to the 2 SAMEBIKE rows, now closed. Verified live in dev server (all 6 pages, header price = body price = highlights, `tsc --noEmit` clean, zero console errors). | 🤖 | Same P0.9-pattern trust rationale, plus a new instance of the P0.30 price-drift bug class in a different form (stale secondary price column instead of stale editorial text) -- catching it before shipping the descriptions avoided baking a second wrong price into the same two pages. **Next candidates:** ~39 more bikes sitewide still have <200-char stub descriptions: SAMEBIKE has 8 left (RS-A07, XD26-II, LO26 Plus, YINYU14, 20LVXD30-II, SY26-II, LOTDM200-II, CY20), plus VTUVIA (~9) and DYU (~7). Continue in brand-sized batches; re-run the `price IS DISTINCT FROM price_usd` check whenever touching a bike's price-referencing copy, since this bug class was silent until this run. |
| 0.34 | ✅ **SAMEBIKE stub-description batch, part 2 (6 of remaining 8 bikes)** (done 2026-07-17 run 29): `WebSearch`/`WebFetch` were rate-limited all run ("session limit, resets 4:50pm"), so the usual samebike.com verification pass wasn't possible. Wrote full editorial descriptions for `samebike-lo26-plus`, `samebike-lotdm200-ii`, `samebike-rs-a07`, `samebike-sy26-ii`, `samebike-xd26-ii`, `samebike-yinyu14` using only their already-populated, non-zero DB fields (torque/weight/battery/range/payload all cross-checked against existing `highlights` arrays, no new claims introduced) -- consistent with the site's standing rule to never fabricate specs. 🆕 **Discovery, NOT resolved:** `samebike-20lvxd30-ii` and `samebike-cy20` have byte-for-byte identical specs across every column (price $699, torque 35, weight 55 lbs, max_weight 330, range 36/56 mi, battery 13Ah) despite being different model names -- deliberately left untouched rather than writing two near-duplicate descriptions off a likely duplicate-row bug. | 🤖 | Continues the P0.33 batch under a tooling constraint (web research unavailable) by falling back to a lower-risk subset of the work (prose depth from already-verified DB data) instead of skipping the run's SEO_LOG-queued item entirely. |
| 0.35 | ✅ **`samebike-20lvxd30-ii` / `samebike-cy20` duplicate-spec question resolved: two real, different products** (done 2026-07-17 run 30). With WebSearch/WebFetch back online, confirmed via samebike.com official product pages that these are genuinely different bikes sharing the same base 250W/35Nm motor spec (SAMEBIKE's common EU-legal baseline) but differing in fold mechanism, tires, and battery: 20LVXD30-II is a classic double-fold frame (handlebars + frame fold together), 20x1.95" tires, 48V 10.4Ah battery, 120 kg (264 lb) payload; CY20 is an ultra-compact 5-second seat-integrated fold, wider 20x2.35" tires, 36V 13Ah (468Wh) battery, 150 kg (330 lb) payload. The DB's shared placeholder values (`max_weight=330` on both, `battery_capacity=13` on both) were a real copy-paste bug -- one row's real spec had overwritten the other's. Fixed `20lvxd30-ii`: `max_weight` 330→264, `battery_capacity` 13→10; fixed `cy20`: `weight_lbs` 55→62 (confirmed via a US retailer listing). Wrote fully differentiated editorial descriptions for both, each explicitly contrasting itself against its sibling. | 🤖 | Closes a second instance of the P0.16-class "which of two real products does this row represent" ambiguity, and catches a genuine copy-paste data bug (wrong payload/battery figures) that a same-value coincidence had been masking since at least run 28. |
| 0.36 | ✅ **VTUVIA stub-description batch, part 1 (3 bikes)** (done 2026-07-17 run 30): `vtuvia-reindeer-2`, `vtuvia-sx20`, `vtuvia-zeal-lt7` were all under 200 chars (single generic sentences) despite having complete, non-zero DB specs. Wrote full editorial descriptions + highlights from the already-verified DB fields (torque/weight/battery/range/payload), each noting how the bike compares to its closest sibling. 5 more VTUVIA bikes remain as stubs: `vtuvia-sn100`, `vtuvia-zeal-xt8`, `vtuvia-cmb`, `vtuvia-sf20h`, `vtuvia-gemini`. | 🤖 | Same rationale as every prior stub-description batch: VTUVIA is now the brand with the most remaining thin-description pages after the SAMEBIKE batches were mostly closed out. **Next candidates:** (1) Finish the remaining 5 VTUVIA stubs (`sn100`, `zeal-xt8`, `cmb`, `sf20h`, `gemini`) -- straightforward, DB fields are already complete and non-zero for all 5. (2) DYU (~7 bikes) stub-description batch still queued, not yet started. (3) `eunorau-defender-s-fat-hs` (P0.16b) still needs a Dylan/human call. (4) Consider adding `tsx` to `package.json` `devDependencies` (flagged run 28, still unresolved). (5) Watch `/best/cargo-ebikes` query-level positions (pos ~51-54 on "best cargo bike(s)" variants per run 29) for continued movement. |

**Exit criteria:** all key pages indexed, methodology + about live, top 6 demand clusters are 800+ words with FAQs, internal links wired.

---

## P1 — Conversion & trust (turn visitors into clicks)

Traffic is worthless if it does not click the affiliate link.

| # | Step | Owner | Why |
|---|------|-------|-----|
| 1.1 | **Bike detail page depth** (partial done 2026-06-28, continued 2026-07-06, 2026-07-07 run 6): Added "Who is this bike for?" section (Best for / Skip if, dynamically generated from bike data), FAQPage schema (4 questions targeting "[brand] [model] review" queries), and spec-forward meta descriptions (`X mi real-world range, Y Nm motor, $Z. Score: N/10`). Applies to all 88+ bike pages. 2026-07-06: deepened the 3 SAMEBIKE RS-A01 variants (Pro/MEN/Plus) — these are the strongest-ranking individual bike pages on the site (MEN pos 9.1, Pro pos 16, both already converting clicks) but had near-identical one-sentence descriptions. Wrote differentiated, manufacturer-sourced (samebike.com) editorial copy for each and fixed 2 data bugs found along the way: RS-A01 MEN's `wheel_size` was 26" but the bike actually ships with 27.5" wheels, and both MEN and Pro were missing their dual front+seat-tube suspension (`has_suspension` was 'front', corrected to 'full'; Plus genuinely is front-only, left as-is). 2026-07-07 run 6: deepened the 3 pages flagged as "still to do" — `engwe-p275-se` (top GSC impressions), `duotts-duotts-c29-k` (dual GSC+PostHog signal, converting clicks), `walfisk-...-wf26` (previously page-1 position) — each rewritten from a single generic sentence to 3-4 sentences of differentiated editorial copy (specific spec callouts, honest manufacturer-vs-real-world range framing, use-case fit), cross-checked against each bike's own DB range/weight/payload columns to avoid introducing new contradictions. 2026-07-08 run 9: deepened `duotts-e29` (recurring GSC click at pos ~18.5, flagged as a decent-but-thin description for two consecutive runs) — rewrote to 4 sentences covering the torque-sensor motor, hydraulic brakes/air shock, claimed-vs-real range (56mi/36mi), and price positioning. | 🤖 | Detail pages are where the affiliate click happens. Make them the strongest pages on the site. |
| 1.2 | ✅ **Comparison tables on best-of pages** (done 2026-07-05): added `SpecComparisonTable` component — a server-rendered "Quick comparison" table (rank, bike, price, range, torque, payload, score) shown above the card grid on every `/best/[category]` page, top 8 bikes by score. Not client-sortable (kept it simple/SSR for the featured-snippet target), but every category now has this. | 🤖 | Comparison tables win featured snippets and keep users on-page. |
| 1.3 | ✅ **AggregateRating schema on detail pages** (done 2026-06-29): Added `aggregateRating` to the Product JSON-LD on all 88 bike detail pages. ratingValue = scoreOverall÷2 (10-point → 5-star), reviewCount=1 (editorial review). Also unified the `reviewRating` inside the Review node to use the same `ratingValue` variable. All pages now eligible for star-rating rich results in SERPs. | 🤖 | Star ratings in search results lift CTR substantially. |
| 1.4 | ✅ **Price & freshness** (done 2026-07-13 run 23): added a "Price and specs last reviewed [date]" line on every bike detail page, sourced from the `ebikes.updated_at` column (bumped by an existing DB trigger on every row edit -- confirmed every one of the 109 rows has a real, non-synthetic timestamp, oldest 2026-05-22). When a row is >90 days stale the copy switches to a "confirm the current price on the official site before buying" nudge instead of a bare date. No schema migration needed; added `updatedAt` to the `EBike` type and `mapRowToEBike`. | 🤖 | Trust signal + avoids sending users to wrong prices; the copy is honest because it reflects a real edit timestamp rather than a claimed re-verification we didn't do. |
| 1.5 | ✅ **CTA consistency audit** (done 2026-07-14 run 24): audited every outbound "Check price" CTA site-wide. Affiliate tagging itself was fully covered everywhere (all URLs pass through `withAffiliateTag` at data-fetch time in `lib/ebike-data.ts`, so commission was never at risk). Found the real gap: click *tracking* was missing. The bike detail page's CTAs correctly use the `AffiliateLink` client component (fires `affiliate_link_clicked` PostHog event), but `/vs/[slug]` comparison pages (9 pages, bottom-of-funnel high-intent) and `/e-bikes/vergelijk` (the compare tool) both had raw `<a href={bike.affiliateUrl}>` CTAs that never fired the tracking event -- meaning any conversions from the two most commercial-intent surfaces on the site were invisible in PostHog. Fixed both to use `AffiliateLink`. Hover rules were already correct (both used `cta-primary`). | 🤖 | Untagged links = zero commission (this was fine); untracked clicks = flying blind on which pages actually convert, which is what P0.26-style "watch for a second signal" decisions depend on. |
| 1.6 | ✅ **Link score badges to `/how-we-test`** (done 2026-06-27): "How we score →" link in Scores section on detail pages; "See our full methodology" link in How We Rank box on best-of pages. BikeCard badge not linked (nested `<a>` invalid HTML — covered by detail page instead). | 🤖 | Reinforces the methodology trust signal and feeds internal links to the new E-E-A-T page. |
| 1.7 | ✅ **Add About to the header nav** (done 2026-06-27) | 🤖 | Makes trust pages reachable from every page, not just the footer. |

---

## P2 — Catalog & affiliate expansion (revenue ceiling)

Today all 7 brands are budget Chinese DTC. The high-search-volume US brands are not in the catalog yet — that caps both traffic and revenue.

| # | Step | Owner | Why |
|---|------|-------|-----|
| 2.1 | **Apply to the big US affiliate programs**: Aventon (AvantLink), Lectric (ShareASale), Rad Power (Impact), Ride1Up (direct), Velotric (Impact) | 👤 | These brands own the US search volume ("aventon aventure review" etc.). Approval takes 1-14 days, so start now. |
| 2.2 | **Add each brand to the catalog once approved** (scrape via the add-brand skill, fill affiliate code in `lib/ebike-data.ts`) | 🤖 | Unlocks dozens of high-intent product + comparison pages. |
| 2.3 | **High-intent vs-pages** (partial, continued 2026-07-12 run 19): added a 4th bike-level matchup, `eunorau-meta-275-st-1-vs-engwe-l20` -- both recurring PostHog signal (3 visitors, 2 consecutive pulls) at an identical $1,399 price with no vs-page yet. 9 vs-pages total now (5 brand-level + 4 bike-level). While building it, found and fixed a live P0.9-pattern bug on `eunorau-meta-275-st-1`: `torque`/`weight`/`max_weight` were all zero (a bike getting real traffic was showing "0 Nm" / "0 lbs" on its own vs-page) and `frame_type` was wrong (`step-over` for a bike whose own description says "ST" = step-through). Sourced real specs from a retailer listing (reallygoodebikes.com) matching the already-verified sibling `eunorau-meta-275-1`: 65 Nm torque, 68.3 lbs, 286 lb payload, step-through frame. Also rewrote its one-line stub description to real editorial copy. Still true once the P2.1 brands land: "X vs Y" pages for brands people actually compare (e.g. Aventon vs Rad, Lectric XP vs Aventon Soltera). | 🤖 | "vs" queries are bottom-of-funnel and convert well. |
| 2.7 | ✅ **AWD/dual-motor blog post** (done 2026-07-04): `/blog/awd-ebikes-explained` — targets "AWD e-bike", "dual motor electric bike" queries; links to DUOTTS S26 (top PostHog product page), off-road best-of, and Eunorau FAT-AWD detail pages. Off-road best-of relatedPosts updated to cross-link. | 🤖 | DUOTTS S26 is the most-visited product page in PostHog. AWD content feeds it link equity and builds topical authority for the off-road cluster. |
| 2.4 | **Expand best-of coverage** ✅ fully done (2026-06-28 through 2026-07-04): ✅ fat-tire, ✅ long-range, ✅ under-$2000, ✅ step-through (`/best/step-through-ebikes` — 3 sections, 7 FAQs, done 2026-06-30), ✅ off-road (`/best/off-road-ebikes` — 3 sections including AWD vs single-motor guide, 7 FAQs, 30+ bikes, done 2026-07-04). All added to sitemap. | 🤖 | Each is a new ranking surface targeting a distinct cluster. |
| 2.6 | ✅ **Folding e-bikes catalog + content (done 2026-06-28)**: The previous "0 bikes" finding was wrong — the filter checks `dimensions.foldedSize`, and 11 Engwe bikes already have it set. The page was showing bikes but had thin content. Fixed: added a 3-section buyer's guide (~900 words) and 7 FAQs to `/best/folding-ebikes`. Page ranks pos 18.8. Outstanding: ENGWE bikes in Supabase have Dutch `suitable_for` values (`woon-werk`, `recreatief`) which hide them from commuter/recreation filters — fixing this is a separate data cleanup task (see P0.5 below). | 🤖 |
| 2.5 | ✅ **More state/city pages for local "ebikes near me / in [city]" intent** (done 2026-07-09 run 13) — now all 50 states live. Progress history: 20 states as of 2026-06, +5 2026-07-08 run 8 (Tennessee, Missouri, Maryland, Wisconsin, Nevada), +5 2026-07-08 run 9 (Indiana, Connecticut, Alabama, South Carolina, Kentucky), +5 2026-07-09 run 10 (Louisiana, Oklahoma, Iowa, Kansas, Arkansas), +5 2026-07-09 run 11 (Idaho, New Mexico, Mississippi, West Virginia, Nebraska), +5 2026-07-09 run 12 (Maine, Montana, North Dakota, South Dakota, Wyoming), final +5 2026-07-09 run 13 (Delaware, New Hampshire, Vermont, Rhode Island, Alaska). Non-three-class outlier states found across the whole sweep: Hawaii, New Jersey (pre-existing), South Carolina, Kentucky, North Carolina (run 9), Montana (run 12), and now **Alaska** (run 13) — Alaska has no enacted statewide e-bike classification law at all (HB 8 vetoed 2023, SB 62 stalled), the only state with no codified class system whatsoever rather than a differently-defined single-tier system. Vermont (run 13) is a genuine standout: one of only 13 US states with zero e-bike helmet mandate at any age or class. Rhode Island (run 13) is the only state applying its under-21 helmet rule to all three classes, not just Class 3. Delaware (run 13) requires Class 3 helmets for all ages, not just under-18. All auto-included in sitemap via `getAllStateSlugs()`. | 🤖 | Local intent is lower competition and easy wins. |

---

## P3 — Engagement & retention (compounding value)

| # | Step | Owner | Why |
|---|------|-------|-----|
| 3.1 | **Email capture + Brevo newsletter** (already in the stack): quiz follow-up sequence + a weekly best-deals email | 🤝 (Claude content, Dylan setup) | Builds an owned audience that does not depend on Google rankings. |
| 3.2 | **Quiz funnel optimization**: tighten the question flow, make the top-3 result page a strong affiliate surface | 🤖 | The quiz is a high-engagement asset; make its result page convert. |
| 3.3 | **Deal / price-drop section**: a lightweight deals page surfacing the best current offers | 🤖 | "ebike deals" is a recurring high-intent query and a reason to return. |

---

## P4 — Scale & moat (once traffic is flowing)

| # | Step | Owner | Why |
|---|------|-------|-----|
| 4.1 | **Backlink building**: guest posts on cycling blogs, get listed in "best ebike site" roundups, Reddit/Quora answers | 👤 | Off-page authority is the ceiling on how high anything ranks. Mostly Dylan's manual effort. |
| 4.2 | **Programmatic expansion**: auto-generate the long tail of category + comparison + location pages from the catalog | 🤖 | Scales ranking surfaces without manual writing. |
| 4.3 | **Content distribution** (YouTube/Pinterest/TikTok) feeding the site | 👤 | Diversifies traffic beyond Google. Out of Claude's lane. |
| 4.4 | **Canada/UK variants** once the US engine works | 🤖 | Reuse the engine for a second market. |

---

## What runs automatically vs what needs Dylan

- **Already automated:** the 5-hourly SEO loop pulls GSC data and ships content-depth + on-page improvements (P0.4, P0.5, P1.1-1.3, P2.3-2.5 over time). It commits and pushes live each run.
- **Needs Dylan and blocks the most upside:**
  1. P0.1 — confirm GSC indexation (5 min, do today).
  2. P2.1 — apply to the major US affiliate programs (do today; approvals are the long pole).
  3. P4.1 — backlinks (ongoing).
- **Best next move:** knock out P0.1 and P2.1 today. Everything else the loop and I can grind through.
